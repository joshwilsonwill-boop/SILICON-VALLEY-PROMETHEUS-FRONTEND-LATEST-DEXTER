version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "UTC"
    open-pull-requests-limit: 10
    labels:
      - "security"
      - "dependencies"
    ignore:
      # Ignore major version bumps — only patch/minor
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
